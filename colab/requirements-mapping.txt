# Minimal dependencies for map-vsic-mcc on Google Colab.
# Excludes surya-ocr, dev tools, and test packages to avoid runtime restart
# and conflicts with Colab pre-installed packages (google-genai, firebase-admin).

pydantic>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
openpyxl>=3.1.0
tqdm>=4.65.0
numpy>=1.24.0
httpx>=0.28.1,<1.0.0
ollama>=0.3.0
